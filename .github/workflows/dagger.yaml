name: dagger
on:
  push:
    branches: [main]

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read  # This is required for actions/checkout

jobs:
  hello:
    name: hello
    runs-on: ubuntu-latest
    steps:
      - name: Call Dagger Function
        uses: dagger/dagger-for-github@8.0.0
        with:
          version: "latest"
          cloud-token: dag_marcos-test_0CjeFW0RzgSToZ7zOgW-LTGxHnute0tnJmXajVfT9QU
      - name: Shell
        run: .help
        shell: dagger {0}
        env:
          DAGGER_CLOUD_TOKEN: dag_marcos-test_0CjeFW0RzgSToZ7zOgW-LTGxHnute0tnJmXajVfT9QU
